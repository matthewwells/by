<div class="section">
<h2>Instructions for adding new Species</h2>
<p>Please add species as you see them if they are not already entered.  Use the British Birds list found in the 'Useful Links' section
	of the side bar to ensure accuracy of names etc.
</p>
<p>
<%= link_to 'New Species', new_bird_path %>
</p>
</div>
<div class="section">
<h2>Species Listing</h2>

<table class="bodyTable">
  <tr>
    <th>Record</th>
    <th>Vernacular name</th>
    <th>International name</th>
    <th>Scientific name</th>
    <th>Category</th>
    <th colspan="3">Options</th>
  </tr>

<% @birds.each do |bird| %>
  <tr class="<%= (bird.seen_by? @me_id) ? 'seen' : cycle('a', 'b') %>">
    <td><%= link_to 'Record Sighting', :controller => :sightings, :action => 'new', :id => bird.id unless (bird.seen_by? @me_id) %></td>
    <td><%=h bird.vernacular_name %></td>
    <td><%=h bird.international_name %></td>
    <td><i><%=h bird.scientific_name %></i></td>
    <td><%=h bird.category %></td>
    <td><%= link_to 'Show', bird %></td>
    <td><%= link_to 'Edit', edit_bird_path(bird) %></td>
    <!--td><%= link_to 'Destroy', bird, :confirm => 'Are you sure?', :method => :delete %></td-->
  </tr>
<% end %>
</table>
<%= link_to 'New Species', new_bird_path %>
</div>


