<h1>Comment on sighting</h1>

<% form_for @sighting do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.submit 'Comment' %>
  </p>

  <p>
    <%= f.label :comments %><br />
    <%= f.text_area :comments %>
  </p>
  <p>
    <%= f.submit 'Comment' %>
  </p>
<% end %>

<%= link_to 'Show', @sighting %> |
<%= link_to 'Back', sightings_path %>